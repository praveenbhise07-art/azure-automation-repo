name: Script Validation CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch code repository
        uses: actions/checkout@v4

      - name: Run Script Analyzer
        shell: pwsh
        run: |
          Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
          
          # Only pull severe issues that would break execution (Errors)
          $errors = Invoke-ScriptAnalyzer -Path ./scripts/ -Recurse | Where-Object Severity -eq "Error"
          
          if ($errors) {
              $errors | Format-Table
              exit 1
          } else {
              Write-Host "✅ Validation complete. No breaking syntax issues found!"
          }
